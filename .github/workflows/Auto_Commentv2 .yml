# Disabled: duplicate of auto-comment.yml; original referenced missing root package.json/yarn build.
name: Auto Commentv2

on:
  workflow_dispatch:

jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use .github/workflows/auto-comment.yml instead."
